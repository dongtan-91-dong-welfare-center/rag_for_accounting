<!-- 이슈 #96 코멘트 초안 — 검토 후 `gh issue comment 96 --body-file docs/measurements/issue96_comment_draft.md` 또는 복붙으로 게시 -->

## ✅ 벤치마크 정확도 베이스라인 실측 완료 (1라운드)

전량 인덱싱(33장, 1042청크) 후 K-GAAP 14건 라이브 측정 수행. 측정 하니스 `scripts/benchmark_baseline.py`, raw baseline_20260614_0021.json(로컬 산출물, gitignore로 미추적), 회의용 종합 `docs/measurements/baseline_meeting_brief.md`.

### 채점 방법론 (결정)
- **현행 `ref in citation_text` 문자열 매칭은 0/14 (0%)** — 라벨(`제18장 18.7조`)이 본문(`#### 18.7`)에 문자열로 없어 구조적으로 실패함을 실증.
- **정규화 조항키(chapter + 문단번호) 매칭으로 전환.** 검색단계(`reranked`)·생성단계(`citations`) 분리 측정.
- **[2026-06-13 결정] 정확도 1순위 = 조항 검색, LLM 답변은 참고용** → NFR-002 헤드라인 = 조항 Hit(특히 "가장 적절한 조항 최상단" = Hit@1/MRR).

### 베이스라인 (14건, k=10, 33장 전량)
| 지표 | 결과 | 비율 |
|---|---|---|
| 생성 조항 Hit@1 (★NFR-002 1차) | 3 / 14 | **21.4%** |
| 생성 조항 Hit@10 | 6 / 14 | 42.9% |
| 검색 조항 Hit@10 | 6 / 14 | 42.9% |
| 검색 조항 MRR | 0.21 | — |
| legacy 문자열(현행) | 0 / 14 | 0.0% |
| is_answerable | 13 / 14 | 92.9% |

> 답변은 92.9% 생성하지만 정확한 조항은 42.9%(@10)·21.4%(@1). **90% 목표와 큰 갭.** 검색 Hit=생성 Hit → 병목은 검색 정밀도(생성 단계 추가 손실 없음).

### ⚠️ 검색 불안정성 (중요)
008(10.22)은 측정 데이터 양에 따라 **8장 miss → 31장 hit → 33장 miss로 진동**. 정답 조항이 검색 top-10 경계선에 있어, 무관 장(ch8·9 등) 추가로 후보가 밀리면 탈락. `USE_RERANKER=false`로 1차 검색 점수만 정렬되는 구조적 불안정성.

### 오답 분석 (8건 miss)
- **A. 같은 장 인접 문단 핀포인트 실패**(004·008·009·013): 정답 바로 옆 문단 인용
- **B. 다른 장 혼동**(006: 중소기업특례 31.8→ch10 / 011: 표시원칙 2.45→ch23): 주제어 쏠림
- **C. gold 라벨 입도 불일치**(001: gold `2.6.5` vs 청크 `2.63~2.65`): 벤치 정답 재검토 필요
- **D. 검색 실패→답변불가**(005: 6.33)

### 개선 후보 (우선순위)
1. **🔝 리랭킹 활성화**(`USE_RERANKER=false`→`true`): Hit@10=43%인데 Hit@1=21% + 008 진동 → 정답 문단 상위 고정·안정화. 저비용·고효과 1순위.
2. 장(chapter) 라우팅/필터 (모드B)
3. 청크 입도/`paragraphs` 메타데이터 (모드A, 채점·검색 동시 개선)
4. 벤치마크 gold 라벨 검증 (모드C)
5. CRAG needs_external — 대부분 CRAG=1이나 검색 결과에 따라 진동, 우선순위 하향

### 회의 결정 필요
1. NFR-002 운영 정의 합격선·k (`clause_exact@10 ≥ 90%` + Hit@1 병행 권고)
2. 조항 입도 정책(`clause_prefix` 채택 여부)
3. 청크 metadata 문단번호 부여
4. LLM-as-judge(Gate B) 범위

체크리스트(채점 방법론 / 실행 환경 / 베이스라인 측정 / 오답 분석) 4항 모두 1라운드 완료. 상세 표·근거는 `docs/measurements/baseline_meeting_brief.md` 참조.

---
_Generated by [Claude Code](https://claude.ai/code)_
