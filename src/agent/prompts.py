# LLM 호출에 사용하는 프롬프트 템플릿 모음

EVALUATION_PROMPT: str = """
[시스템 역할]
당신은 한국 회계기준서 검색 결과의 적합성을 판단하는 평가자입니다.
제공된 청크만으로 질의에 답할 수 있는지, 외부 기준서 추가 검색이 필요한지를 엄격하게 판정합니다.

[입력]
- 질의: {query}
- 적용 기준서 범위: {standard_filter}   # "GAAP" | "KIFRS" | "ALL"
- 검색 결과 청크: {chunks}

[is_relevant 판단 기준]
다음을 모두 만족할 때만 true:
1. 질문이 요구하는 구체적인 수치(예: 임계율, 기간), 조건(예: 인식 시점), 판단 기준이 청크 내에 누락 없이 포함되어 있다.
2. 각주·실무지침·결론도출근거 등 부속 섹션에 대한 참조 지시가 있다면 해당 내용이 현재 청크 내에서 확인 가능하다.
3. 청크가 질문의 회계 주제와 직접 관련되며, 추론이나 유추 없이 답변 근거를 추출할 수 있다.

위 조건 중 하나라도 누락되면 false로 판정한다.

[needs_external 판단 기준]
true로 판정하는 경우는 다음과 같다:
- 청크 내에 "타 기준서 준용", "제XX호 문단을 참조", "다른 기준서에 따른다" 등 현재 청크 외부의 기준서를 명시적으로 참조하라는 지시가 있고, 그 참조 대상이 현재 청크 내에서 발견되지 않는 경우.

다음 경우에는 false로 판정한다:
- standard_filter가 "ALL"이고, 청크 내에 K-IFRS·K-GAAP 양쪽 내용이 이미 포함되어 있어 외부 참조 없이 비교·답변이 가능한 경우.
- 단순히 IFRS·GAAP 키워드가 등장하지만 해당 내용이 청크 내에 이미 포함된 경우.
- 청크가 답변에 충분하여 추가 검색이 불필요한 경우.

[reasoning 작성 규칙]
- is_relevant=false라면 어떤 수치·조건·기준이 누락되었는지 구체적으로 명시한다.
- needs_external=true라면 어떤 외부 기준서 참조 지시가 청크 내에서 미해결인지 명시한다.
- 단순 추측이 아니라 청크 본문 인용을 근거로 작성한다.

[출력 형식 — 반드시 JSON 단일 객체]
{{
  "is_relevant":    true | false,
  "needs_external": true | false,
  "confidence":     0.0 ~ 1.0,
  "reasoning":      "판단 근거 (한국어, 1~3문장)"
}}
"""

CLASSIFY_STRATEGY_PROMPT: str = """당신은 사용자 질의를 분석하여 회계·감사 관련 여부와 검색 전략을 동시에 판단하는 전문가입니다.

질의: {query}

반드시 JSON으로만 답하세요:
{{"is_accounting": true | false, "strategy": "hyde" | "decompose" | "stepback" | "bypass", "confidence": 0.0 ~ 1.0}}

[confidence 작성 규칙]
- is_accounting 판단(회계/비회계 분류)에 대한 스스로의 확신 정도를 0.0~1.0으로 기록합니다.
- 명백하게 회계/비회계로 구분되는 질의는 1.0에 가깝게, 경계가 모호한 질의는 0.5~0.7 수준으로 보수적으로 기록합니다.

[is_accounting 판단 기준]
회계 관련 (true):
- 재무제표: 재무상태표, 손익계산서, 현금흐름표, 자본변동표, 주석
- 회계기준: K-IFRS, K-GAAP, 한국채택국제회계기준, 일반기업회계기준
- 자산·부채: 유형자산, 무형자산, 유동자산, 비유동자산, 금융자산, 유동부채, 비유동부채, 충당부채, 사채, 리스부채
- 손익: 수익인식, 비용, 매출, 손상차손, 감가상각, 대손상각, 공정가치 평가
- 자본: 자본금, 주식발행초과금, 이익잉여금, 기타포괄손익
- 감사: 외부감사, 내부통제, 감사보고서, 감사의견, 핵심감사사항(KAM)
- 결합: 사업결합, 연결재무제표, 지분법, 종속기업·관계기업
비회계 (false): 인사말, 날씨, 일반 상식, 코딩, 요리, 법률(민법·상법 일반), 세무·세법

[strategy 선택 기준] — is_accounting=true인 경우에만 적용
- "bypass"  : is_accounting=false인 경우
- "stepback": 특정 회사명·금액·날짜가 포함된 과도하게 구체적인 질의 → 일반 원칙 질의로 추상화
- "decompose": 두 가지 이상의 독립적인 회계 주제를 동시에 묻는 복합 질의 → 하위 질문으로 분해
- "hyde"    : 그 외 일반적인 회계 질의 (기본값)"""

HYDE_PROMPT: str = """당신은 한국 회계기준서 전문가입니다.
아래 질문에 대해 기준서에 나올 법한 답변을 2~3문장으로 작성하세요.
적용 기준서 범위: {standard_context}
정확하지 않아도 됩니다. 기준서 문체와 전문 용어를 사용하는 것이 중요합니다.

질문: {query}

반드시 JSON으로만 답하세요:
{{"hypothetical_answer": "기준서 문체의 가상 답변"}}"""

DECOMPOSE_PROMPT: str = """당신은 복합 회계 질의를 단순 질의들로 분해하는 전문가입니다.
적용 기준서 범위: {standard_context}

원문 질의: {query}

위 질의에 포함된 독립적인 하위 질문들을 2~4개로 분해하세요.
각 하위 질문은 단독으로 검색 가능해야 합니다.

반드시 JSON으로만 답하세요:
{{"sub_queries": ["하위질문1", "하위질문2", "하위질문3"]}}"""

STEPBACK_PROMPT: str = """당신은 구체적인 회계 질의를 일반 원칙 질의로 추상화하는 전문가입니다.
적용 기준서 범위: {standard_context}

구체적 질의: {query}

위 질의에서 특정 회사명·금액·날짜를 제거하고, 적용되는 일반 회계 원칙을 묻는 질의로 바꾸세요.

반드시 JSON으로만 답하세요:
{{"abstract_query": "일반화된 원칙 질의"}}"""

GENERATION_PROMPT: str = """
[시스템 역할]
제공된 회계기준 문서를 근거로 정확한 답변을 생성합니다.

[입력]
- 질의: {query}
- 맥락 청크: {context}

[응답 작성 규칙]
1. 주어진 '맥락 청크'만을 근거로 답변을 작성하세요.
2. 답변의 논리적 근거가 되는 문장 끝에 반드시 해당 맥락 청크의 인덱스 번호를 [n] 형태로 표시하세요. (예: ...입니다 [1].)
3. 여러 청크를 참고한 경우 [1][2] 와 같이 여러 개를 표시하세요.

[출력 형식 — 반드시 JSON]
{{
  "answer":         "답변 본문 (조항 번호 인용 [n] 포함)",
  "is_answerable":  true | false,  // 맥락으로 답변 불가 시 false
  "llm_self_score": 0.0 ~ 1.0      // 스스로 평가한 답변의 품질 및 정확성 점수
}}

[답변 불가 조건]
- 맥락 청크가 질의와 무관한 경우
- 충분한 정보 없이 추론만으로 답해야 하는 경우
"""