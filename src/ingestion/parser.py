"""
문서 파싱 및 청킹 모듈 (구현 예정)

이 모듈은 다양한 형식의 문서(PDF, HWP 등)를 읽어서
검색 가능한 단위(chunk)로 분할하는 역할을 담당할 예정입니다.

TODO LIST:
- [ ] FUNC-001: 문서 텍스트 및 구조 추출 로직 구현 (PDF/HWP -> 마크다운 등)
    - 현재 src/parse/parser.py에 PDF→마크다운 변환은 구현되어 있으며,
      이 모듈은 그 위에 추가적인 파싱 파이프라인을 구성할 예정입니다.
    - 지원하지 않는 확장자 또는 파일 손상 시 적절한 예외(Error) 반환 처리

- [ ] FUNC-002: 계층형 노드 청킹 로직 구현
    - 추출된 텍스트를 장/절/문단 단위의 부모-자식 트리 구조(Node 객체)로 분할
    - 청킹(chunking)이란?
        문서 전체를 한 번에 LLM에 넣으면 너무 길어서 처리할 수 없으므로,
        적절한 크기의 조각(chunk)으로 나누는 과정입니다.
    - 계층형 노드란?
        단순히 글자 수로 자르는 대신, 문서의 구조(장 > 절 > 문단)를 반영하여
        의미 단위로 나누는 방식입니다. 이렇게 하면 검색 시 더 정확한 결과를 얻습니다.
"""
